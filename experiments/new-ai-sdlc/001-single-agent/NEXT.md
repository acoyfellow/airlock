# Next intake

Do not start the eight-agent tier.

Five measured outcomes remain in the denominator. Run `aomkje` reached a working preview
but failed clean replay because `.terrarium-workspace` contaminated its digest. Do not
start the eight-agent tier.

The next invocation must pin the full provider/model tuple:

```text
provider = opencode.cloudflare.dev
model = gpt-5.6-terra
```

The eventual eight-agent run must inherit SEAL.v4's exact provider/model tuple, held-out
idea and checker digests, starting commit, two-million-token and $10 global ceilings,
timer boundaries, and caller-owned release boundary. It may not observe any prior baseline commit or implementation.

Before another baseline, implement and deterministically preflight the prospective
finalizer from review `ter_20260711204604077_q14kpi`. It must derive identity from the
immutable produced commit in a clean detached worktree; retain content-addressed raw
receipt, usage, check, replay, proof, deploy, and response artifacts; use only a
pre-sealed signer; require the exact preview hostname and deployment version; verify the
served source marker; and record request-only production semantics. Seal the finalizer,
source-identity algorithm, exact checks, trust anchor, and preview policy in `SEAL.v5.json`
before launching another model worker.
