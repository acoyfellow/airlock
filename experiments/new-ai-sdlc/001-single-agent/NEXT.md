# Next intake

Do not start the eight-agent tier.

The first measured run is permanently failed and remains in the denominator. After
mutation 20 and its review land, start a new independent baseline run against the same
sealed idea, acceptance checker, starting commit, budgets, and timer boundaries.

The next invocation must pin the full provider/model tuple:

```text
provider = opencode.cloudflare.dev
model = gpt-5.6-terra
```

Before launch, dry-run the exact supervisor command and prove the Pi JSON stream reports
the same provider and model. The usage meter must reject ambiguity rather than merely
recording a model name. This is a new measured run, not a retry of
`ter_20260711165723012_qi3ru8`; both outcomes remain in aggregate baseline accounting.
